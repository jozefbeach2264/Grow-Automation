# Core -- required for poller / cloud control / AI advisor
requests>=2.31
python-dotenv>=1.0

# Optional: local BLE layer (ble_logger.py and aci_ble_lab/).
# Not needed if you only use the cloud transport. Install with:
#   pip install -r requirements.txt --extra-index-url ...  (or just the two below)
bleak>=0.21
ac-infinity-ble>=0.4.3
